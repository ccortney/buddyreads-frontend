import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#3949ab", 
            light: "#9fa8da"
        }, 
        secondary: {
            main: "#ffa000"
        }
    }
    
})

export default theme;

// 