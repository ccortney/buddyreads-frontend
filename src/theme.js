import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#003F7D"
        }, 
        secondary: {
            main: "#ff8E00"
        }, 
        success: {
            main: "#4caf50"
        }, 
        error: {
            main: "#ef5350"
        }
    }
})

export default theme;

// 